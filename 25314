#include <string>
#include <iostream>

using namespace std;

int main(void){
    ios::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);
int byte_n = 0;
cin>>byte_n;
for(int i=0;i<(byte_n/4);i++){
    cout<<"long ";
}
cout<<"int"<<endl;
}
