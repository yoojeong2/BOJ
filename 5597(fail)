#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);
   
    vector<int> v;
   int tmp;
    for(int i=0;i<28;i++){
        cin>>tmp;
       v.push_back(tmp);
    }
    sort(v.begin(),v.end());
    int cnt = 0;
    for(int i = 0;i<28;i++){
        if(cnt==2)break;
        
        if((i + 1 + cnt) != v[i]) // 현재 숫자와 출석번호 일치하지 않으면 이전 데이터 존재 X
        {
            cnt += 1;
            cout<<v[i]-1<<endl;
        }
    }
}
