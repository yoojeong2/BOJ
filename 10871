#include <iostream>


using namespace std;

int main()
{
   ios::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
    int n,m;

    cin>>n>>m;
    int tmp;
    for(int i=0;i<n;i++){
        cin>>tmp;
       if(tmp < m)cout<<tmp<<" ";
    }
    cout<<endl;

}
