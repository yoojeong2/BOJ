#include <iostream>
using namespace std;

int main(void){
    int sum;
    int N, a,b;
    int realSum = 0;
    cin>>sum;
    cin>>N;
    for(int i=0;i<N;i++){
        cin>>a>>b;
        realSum += (a*b);
    }
    if(realSum == sum)cout<<"Yes"<<endl;
    else cout<<"No"<<endl;
}
